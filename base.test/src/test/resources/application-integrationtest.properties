#By the way, this configuration is not necessary because mongodb always knows to connect by default to 'test' database.
spring.data.mongodb.uri=mongodb://localhost/test
spring.data.mongodb.auto-index-creation = true
